#include "../BufferManager/BufferManager.h"

void BufferManager::init() {}

BlockNode* BufferManager::getBlockHead(string fileName) {
    BlockNode* bn = new BlockNode();
    return bn;
}

BlockNode* BufferManager::getNextBlock(string fileName, BlockNode* block) {
    BlockNode* bn = new BlockNode();
    return bn;
}

BlockNode* BufferManager::getBlock(string filename, int offset) {
    BlockNode* bn = new BlockNode();
    return bn;
}